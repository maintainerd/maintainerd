/**
 * Validation helpers index.
 *
 * The core console validates forms inline with react-hook-form rules; the shared
 * regex/sanitizer helpers used by input components live in `./regex`.
 */
export * from './regex'
